# 美团跑腿体验优化：设计系统规范

> 本文是项目稳定的设计事实源，用于定义已确认的 Design Token、文字样式、组件规范和通用还原原则。产品与交互规则见 `product.md`，工程实现规则见 `tech.md`；具体页面视觉以最新 Figma 为准。
>
> Figma 组件库：`UX学习记录`
> 组件库根节点：`960:7565`

## 1. 设计理念

### 1.1 品牌氛围定位
美团跑腿代办重设计的整体气质，不是冷静的 B2B 工具，而是一款**用美团黄把“找人跑腿”这件看不见的事变成“看得见、找得到、信得过”的亲切移动服务**。
这种氛围的关键是“亲切而不廉价，热闹而不乱”：黄色很满，但只作为顶带背景和主按钮；吉祥物增加了人格感，而不是冷冰冰的图标；所有操作都集中在一张大白卡里，不让用户在大片黄色里迷路。整体感觉像一个话不多、但装备整齐的跑腿小哥——一眼就能认出来，下一步该点什么也很清楚。

- 黄色顶带 + 吉祥物作为首屏品牌锚点。
- 白色大圆角卡片浮在黄色背景上，形成清晰的“品牌层 / 任务层”分离。
- 移动优先：所有内容以窄屏卡片为骨架，操作贴近拇指。
- 热闹但有序：黄色是情绪，红/绿是状态，黑/灰是信息。

### 1.2 标志性视觉手法
这套设计的招牌手法可以概括为：**“用美团黄顶带和吉祥物建立即时品牌感，再用白色大圆角卡片把复杂的委托流程装成一连串可滑动、可点击、可追踪的任务层”**。
通用设计原则：

- 语义优先于装饰。
- 关键状态保持清晰、连续、可识别。
- 信息层级依靠字重、颜色和间距共同建立。
- 公共组件优先于页面内重复绘制。

## 2. 基础规范

### 2.1 颜色

表中的“代码名”是工程映射名，“Figma Style”是设计源名称。新增颜色前应先判断是否出现了新的稳定语义。

| 代码名 | Figma  | 值 | 语义 |
|---|---|---:|---|
| `brand-primary` | `主题色/brand-primary` | `#FEE42B` | 品牌锚点、主操作、明确选中态 |
| `brand-bg` | `主题色/brand-bg` | `#FFFBE4` | 品牌浅底、轻量选中背景 |
| `highlight-primary` | `主题色/highlight-primary` | `#F58B1D` | 需要优先注意的信息 |
| `highlight-bg` | `主题色/highlight-bg` | `#FFF5E9` | 强调信息的浅色承载面 |
| `text-primary` | `中性色/text-primary` | `#1B1D21` | 一级文字与主要图形 |
| `text-secondary` | `中性色/text-secondary` | `#595D65` | 二级文字 |
| `text-tertiary` | `中性色/text-tertiary` | `#8C9098` | 三级文字、辅助信息 |
| `text-quaternary` | `中性色/text-quaternary` | `#DDE0E6` | 禁用、弱提示和浅描边 |
| `page-bg` | `中性色/page-bg` | `#F4F5F7` | 页面背景 |
| `container-bg` | `中性色/container-bg` | `#FBFCFE` | 卡片和容器背景 |
| `mask-bg` | `中性色/mask-bg` | `#282B31` | 深色面、气泡和遮罩基色 |
| `border-divider` | `中性色/border-divider` | `rgba(27,29,33,0.05)` | 轻量分隔与描边 |
| `decorative-primary` | `辅助色/decorative-primary` | `#804D30` | 装饰性深色文字 |
| `decorative-secondary` | `辅助色/decorative-secondary` | `#FDD4B8` | 装饰性浅棕 |
| `decorative-tertiary` | `辅助色/decorative-tertiary` | `#FFF4EE` | 装饰性浅底 |
| `alert-primary` | `辅助色/alert-primary` | `#FF090D` | 警示与强提醒 |
| `insurance-primary` | `辅助色/insurance-primary` | `#079968` | 保价与保障语义 |
| `insurance-bg` | `辅助色/insurance-bg` | `#E7FFF2` | 保价与保障浅底 |

说明：

- `insurance-*`、`alert-primary` 是具名功能色，不自动扩展为全局成功色或错误色。
- `decorative-*` 只用于装饰和特定权益表达，不承担通用交互状态。
- 单个插画或组件内部渐变不因复用方便而自动上升为全局颜色 Token。
- 禁止从截图目测或自行混色；Figma 已存在的颜色必须使用对应 Token。

### 2.2 字体

字体分工：

- **PingFang SC**：中文标题、正文、页签和注释。
- **SF Pro**：价格、时间、里程、评分和展示数字。
- **Meituan Type**：品牌文字和装饰性文字。

当前常规 Text Style 的 Figma 行高为 `Auto`，代码映射为字体的正常行高；不得继续沿用旧文档中的统一 `1.4` 或 `1.5`。只有表中明确给出像素值的样式使用固定行高。

| 代码名 | Figma Text Style | 字体 | 字号 | 字重 | 行高 | 字距 |
|---|---|---|---:|---:|---:|---:|
| `headline` | `标题/headline` | PingFang SC | 22px | 500 | Auto | 0 |
| `title` | `标题/title` | PingFang SC | 18px | 600 | Auto | 0 |
| `title-sm` | `标题/title-sm` | PingFang SC | 16px | 600 | Auto | 0 |
| `title-max` | `标题/tilte-max` | PingFang SC | 30px | 600 | Auto | 0 |
| `title-brand` | `标题/title-brand` | Meituan Type | 20px | 400 | Auto | 0 |
| `tab-active` | `标签/tab-active` | PingFang SC | 16px | 600 | Auto | 0 |
| `tab` | `标签/tab` | PingFang SC | 16px | 400 | Auto | 0 |
| `body` | `正文/body` | PingFang SC | 14px | 500 | Auto | 0 |
| `body-secondary` | `正文/body-secondary` | PingFang SC | 14px | 400 | Auto | 0 |
| `caption-strong` | `注释/caption-strong` | PingFang SC | 12px | 500 | Auto | 0 |
| `caption` | `注释/caption` | PingFang SC | 12px | 400 | Auto | 0 |
| `caption-icon` | `注释/caption-icon` | PingFang SC | 12px | 600 | Auto | 0 |
| `caption-sm` | `注释/caption-sm` | PingFang SC | 11px | 400 | Auto | 0 |
| `caption-xs-strong` | `注释/caption-xs-strong` | PingFang SC | 10px | 500 | Auto | 0 |
| `caption-xs` | `注释/caption-xs` | PingFang SC | 10px | 400 | Auto | 0 |
| `number` | `数字/number` | SF Pro | 14px | 510 | Auto | 0 |
| `number-sm` | `数字/number-sm` | SF Pro | 12px | 400 | Auto | 0 |
| `number-xs` | `数字/number-xs` | SF Pro | 11px | 400 | Auto | 0 |
| `number-lg` | `数字/number-lg` | SF Pro | 16px | 700 | Auto | 0 |
| `display` | `数字/display` | SF Pro | 24px | 700 | 20px | 0 |
| `display-lg` | `数字/display-lg` | SF Pro | 32px | 510 | Auto | 0 |
| `display-xl` | `数字/display-xl` | SF Pro | 36px | 700 | 36px | 0 |
| `decorative` | `装饰/decorative` | Meituan Type | 12px | 700 | Auto | 0 |

说明：

- Figma 中 `标题/tilte-max` 当前存在拼写错误；代码名保持为 `title-max`，同步设计库时应优先修正源名称。
- 一个复杂组件可以包含多种文字 Token，不能给整个组件指定一个笼统的 Typography。
- 只有跨变体保持稳定的文字槽位，才在组件实现中固定引用某个文字 Token。
- 字体还原必须同时核对字体、字号、字重、行高、字距和颜色。

### 2.3 间距

Figma Variable Collection：`Spacing 间距`

| Token | 值 |
|---|---:|
| `space-2` | 2px |
| `space-4` | 4px |
| `space-6` | 6px |
| `space-8` | 8px |
| `space-10` | 10px |
| `space-12` | 12px |
| `space-16` | 16px |
| `space-20` | 20px |
| `space-24` | 24px |

规则：

- 基础栅格为 4px，允许 2px 半档和 10px 节奏。
- `page gutter` 指页面最外层左右安全距离，全工程统一使用 `space-8`。
- `inter-component gap` 指同一父级下相邻组件之间的间距，默认使用 `space-8`；纵向堆叠时也可称为 `stack gap`。
- `padding` 指组件内部边距，不等同于 `page gutter` 或 `inter-component gap`。
- 组件内部 padding 和 gap 属于组件自身；组件外边距由父级布局负责。
- Figma 当前实例的绝对坐标不是可复用间距 Token。

### 2.4 圆角

Figma Variable Collection：`Radius 圆角`

| Token | 值 |
|---|---:|
| `radius-2` | 2px |
| `radius-4` | 4px |
| `radius-6` | 6px |
| `radius-8` | 8px |
| `radius-10` | 10px |
| `radius-12` | 12px |
| `radius-16` | 16px |
| `radius-20` | 20px |
| `radius-full` | 9999px |

规则：

- 小圆角用于标签、角标和细节。
- 中等圆角用于输入框、图片框和普通卡片。
- 大圆角用于主容器和面板。
- `radius-full` 用于胶囊、头像和圆形标记。
- 已绑定 Token 的圆角不得通过目测近似。

### 2.5 响应式

- 以移动端 App 为主要目标，不对整张页面做等比例缩放。
- 屏幕宽度小于或等于 `448px` 时，页面铺满可用宽度。
- 屏幕宽度大于 `448px` 时，应用保持约 `448px` 宽，并在桌面浏览器中水平居中。
- 页面最外层左右安全距离统一使用 `space-8`；它不等于组件内部 padding。
- 普通内容区域默认 `width: Fill`、`height: Hug`，高度由内部内容决定。
- 输入框、按钮、页签、导航栏等稳定交互控件可以固定高度。
- 图片、地图和插画可以使用固定展示区或宽高比，但必须保持原始比例。
- 当前 Figma 实例尺寸只作为 `referenceSize`，不直接转换为所有屏幕通用的固定宽高。
- 字号、字重和图标尺寸默认不随视口线性缩放；优先通过 Fill、间距、换行和裁切适配。
- 文本变长时按设计语义换行或截断，不压缩字号，不遮挡相邻内容。
- 页面高度随内容增长；需要占满屏幕时使用最小高度，不锁定整页固定高度。
- 固定在视口底部的操作区必须预留系统安全区和 Home Indicator 空间。
- 至少检查 `320px`、`375px`、`390px`、`414px`、`448px` 五种宽度。
- 重点检查页面边距、文本换行、横向溢出、固定栏覆盖、素材比例和安全区。

## 3. 公共组件契约

### 阅读约定

- `Fixed`：组件在该轴上具有稳定的交互尺寸或比例。
- `Hug`：组件在该轴上由内部内容、变体或数据决定尺寸。
- `Fill`：组件在该轴上填充父级提供的可用空间。
- 下列尺寸行为是结合组件结构与实际使用语义得到的实现契约，不机械照抄组件库中未严格维护的旧 Resizing 设置。
- `referenceSize` 仅用于核对当前设计密度，不作为代码固定值。
- 本节不记录组件在具体页面中的外边距、绝对位置、显隐逻辑或业务触发条件。

### NavigationBar

#### 组件身份

- 名称：NavigationBar
- Figma Component Set：`856:1634`
- 职责：提供应用顶部的返回、标题、位置、天气和辅助操作容器。

#### 尺寸行为

- 宽度：Fill
- 高度：Fixed
- referenceSize：`375 × 44px`

#### 变体与槽位

- 变体轴 `Property 1`：首页、物品信息页、取地址信息页、收地址信息页、订单确认页、订单确认页/上滑、物流状态页。
- 可替换槽位：左侧操作、中心标题或公告、品牌与位置、天气、右侧辅助操作。
- 稳定差异：不同变体改变导航语义和槽位组合，不改变导航栏的基础高度。

### 首页业务选择

#### 组件身份

- 名称：首页业务选择
- Figma Component Set：`911:6215`
- 职责：承载帮送、帮取与 1 对 1 急送的核心下单信息。

#### 尺寸行为

- 宽度：Fill
- 高度：Fixed
- referenceSize：`359 × 324px`

#### 变体与槽位

- 变体：帮送、帮取、1对1急送。
- 可替换槽位：运力信息、取件地址、收件地址、地址簿操作、价格、优惠标签、主按钮、隐私说明。
- 稳定差异：业务模式改变页签选中态、地址语义和服务表达，核心表单结构保持一致。

### 载具切换胶囊

#### 组件身份

- 名称：载具切换胶囊
- Figma Component Set：`904:7346`
- 职责：在两轮车与汽车配送方式之间切换。

#### 尺寸行为

- 宽度：Fixed
- 高度：Fixed
- referenceSize：`158 × 32px`

#### 变体与槽位

- 变体：两轮车、汽车。
- 可替换槽位：无自由内容槽位；选项图标和文字属于稳定语义。
- 稳定差异：仅改变选中滑块、图标和文字的激活状态。

### 额外业务

#### 组件身份

- 名称：额外业务
- Figma Component：`1677:9637`
- 职责：展示一组次级业务入口。

#### 尺寸行为

- 宽度：Fill
- 高度：Fixed
- referenceSize：`359 × 85px`

#### 变体与槽位

- 变体：无。
- 可替换槽位：五个业务入口的图标与标题。
- 稳定差异：入口数量和排列结构固定，内容可替换。

### Tab Bar

#### 组件身份

- 名称：Tab Bar
- Figma Component：`1677:10022`
- 职责：提供应用一级底部导航并预留系统 Home Indicator 空间。

#### 尺寸行为

- 宽度：Fill
- 高度：Fixed
- referenceSize：`375 × 80px`

#### 变体与槽位

- 变体：当前组件未建立变体，设计实例为“跑腿”选中态。
- 可替换槽位：三个导航项的图标和标题。
- 稳定差异：选中态应由导航状态控制，不能通过替换整张图片实现。

### 地址识别

#### 组件身份

- 名称：地址识别
- Figma Component：`885:6238`
- 职责：提供粘贴地址并自动识别的入口。

#### 尺寸行为

- 宽度：Fill
- 高度：Fixed
- referenceSize：`359 × 48px`

#### 变体与槽位

- 变体：无。
- 可替换槽位：相机图标、提示文字、右侧粘贴操作。
- 稳定差异：左提示与右操作的结构固定。

### 地址簿

#### 组件身份

- 名称：地址簿
- Figma Component：`885:5885`
- 职责：承载地址搜索、地址管理提示和可重复地址条目。

#### 尺寸行为

- 宽度：Fill
- 高度：Hug
- referenceSize：当前示例约 `359 × 597px`

#### 变体与槽位

- 变体：无。
- 可替换槽位：标题、搜索内容、操作提示、地址头像、地址、联系人、电话和编辑操作。
- 稳定差异：地址条目为可重复槽位，组件高度随条目数量增长。

### 地址填写

#### 组件身份

- 名称：地址填写
- Figma Component Set：`1082:9471`
- 职责：编辑取件或收件地址及联系人信息。

#### 尺寸行为

- 宽度：Fill
- 高度：Hug
- referenceSize：未填写约 `359 × 269px`，已填写约 `359 × 412px`

#### 变体与槽位

- 变体轴 `取收地`：取件、收件。
- 变体轴 `填地址`：未填、已填。
- 可替换槽位：地图、地址、门牌号、联系人、电话、辅助操作和保存按钮。
- 稳定差异：取收语义改变标识；填写状态决定完整表单是否展开。

### 物品类型选择

#### 组件身份

- 名称：物品类型选择
- Figma Component Set：`873:5405`
- 职责：选择物品分类并承载照片识别、备注和保价区域。

#### 尺寸行为

- 宽度：Fill
- 高度：Hug
- referenceSize：未选择约 `359 × 383px`，已选择约 `359 × 276px`

#### 变体与槽位

- 变体轴 `Property 1`：未选择、已选择。
- 可替换槽位：分类标题、禁送提示、照片识别、分类网格、已选类型、图片、备注、`Slot 2` 保价区域。
- 稳定差异：未选择态展示分类网格；已选择态收敛为摘要并显示后续信息。

### 物品重量

#### 组件身份

- 名称：物品重量
- Figma Component：`877:5432`
- 职责：展示并调整物品重量。

#### 尺寸行为

- 宽度：Fill
- 高度：Fixed
- referenceSize：`359 × 180px`

#### 变体与槽位

- 变体：无。
- 可替换槽位：标题、帮助入口、说明、当前重量、刻度、轨道和滑块。
- 稳定差异：数值与滑块位置可变，整体结构稳定。

### 物品体积

#### 组件身份

- 名称：物品体积
- Figma Component Set：`937:7732`
- 职责：展示配送箱参考尺寸，并提供两种详细三维尺寸编辑方式。

#### 尺寸行为

- 宽度：Fill
- 高度：Hug
- referenceSize：标准尺寸 `359 × 194px`；详细尺寸 1 `359 × 664px`；详细尺寸 2 `359 × 480px`

#### 变体与槽位

- 变体轴 `Property 1`：标准尺寸、详细尺寸1、详细尺寸2。
- 可替换槽位：标题、详细尺寸开关、说明、配送箱参考、骑手语录、长宽高数值、刻度或三轴控制、恢复默认。
- 稳定差异：标准态展示参考箱；详细尺寸1使用三组滑杆；详细尺寸2使用三轴直接操控。

### 物品确认按钮

#### 组件身份

- 名称：物品确认按钮
- Figma Component Set：`880:6260`
- 职责：提供物品信息流程的底部确认操作。

#### 尺寸行为

- 宽度：Fill
- 高度：Fixed
- referenceSize：操作栏 `375 × 88px`，内部按钮高 `44px`

#### 变体与槽位

- 变体轴 `Property 1`：按钮正常、按钮置灰。
- 可替换槽位：按钮文字。
- 稳定差异：正常与禁用状态改变背景和文字颜色，尺寸不变。

### 进度条

#### 组件身份

- 名称：进度条
- Figma Component Set：`842:1359`
- 职责：表达订单从接单到收件的阶段进度。

#### 尺寸行为

- 宽度：Fill
- 高度：Fixed
- referenceSize：约 `335–336 × 30px`

#### 变体与槽位

- 变体轴 `Property 1`：等待接单、等待取件、等待收件、已收件。
- 可替换槽位：无自由内容槽位；节点语义和顺序固定。
- 稳定差异：当前节点、已完成连接线和待完成节点的状态变化。

### 订单信息

#### 组件身份

- 名称：订单信息
- Figma Component：`942:7862`
- 职责：汇总取收地址、联系人、备注、实付款和订单明细入口。

#### 尺寸行为

- 宽度：Fill
- 高度：Hug
- referenceSize：当前示例宽 `359px`

#### 变体与槽位

- 变体：无。
- 可替换槽位：取件地址、收件地址、联系人、电话、备注、实付款和明细入口。
- 稳定差异：字段语义和排列固定，文本内容与高度随数据变化。

### 下单页其余操作

#### 组件身份

- 名称：下单页其余操作
- Figma Component：`955:8135`
- 职责：承载取件时间、收货码和小费等次级设置。

#### 尺寸行为

- 宽度：Fill
- 高度：Hug
- referenceSize：当前示例宽 `359px`

#### 变体与槽位

- 变体：无。
- 可替换槽位：行标题、当前值或提示、右侧图标。
- 稳定差异：三行列表结构固定，内容可由业务数据替换。

### 缩略地图

#### 组件身份

- 名称：缩略地图
- Figma Component：`942:8668`
- 职责：以紧凑形式展示取收地址并提供互换入口。

#### 尺寸行为

- 宽度：Fill
- 高度：Fixed
- referenceSize：`359 × 52px`

#### 变体与槽位

- 变体：无。
- 可替换槽位：取件地址、收件地址、联系人、电话和互换操作。
- 稳定差异：左右地址块、取收标签和中间互换结构固定。

### 高信用骑士

#### 组件身份

- 名称：高信用骑士
- Figma Component：`955:7754`
- 职责：展示保价服务所提供的骑手与赔付保障。

#### 尺寸行为

- 宽度：Fill
- 高度：Hug
- referenceSize：当前示例宽 `375px`

#### 变体与槽位

- 变体：无。
- 可替换槽位：保障图标、保障文案和右侧入口。
- 稳定差异：保障语义和浅绿色承载面固定。

### 动态催促标签

#### 组件身份

- 名称：动态催促标签
- Figma Component Set：`862:4273`
- 职责：在稳定高度内展示运力、保障、优惠或热评动态信息。

#### 尺寸行为

- 宽度：Fill
- 高度：Fixed
- referenceSize：外层 `375 × 44px`，内部标签宽度 Hug

#### 变体与槽位

- 变体轴 `Property 1`：0、1、2、3、4、5。
- 可替换槽位：状态图标、主文案、强调文案、倒计时或数量。
- 稳定差异：`0` 为隐藏占位态；其余状态分别表达运力、保障、限时、折扣和热评语义。

### 取收标签

#### 组件身份

- 名称：取收标签
- Figma Component Set：`942:8552`
- 职责：以最小视觉单元标识取件或收件及普通或尊贵等级。

#### 尺寸行为

- 宽度：Fixed
- 高度：Fixed
- referenceSize：`22 × 22px`

#### 变体与槽位

- 变体轴 `Property 1`：取/普通、收/普通、取/尊贵、收/尊贵。
- 可替换槽位：无自由文字槽位。
- 稳定差异：取收语义与普通/尊贵视觉等级组合成四种状态。

### 取收地图标签

#### 组件身份

- 名称：取收地图标签
- Figma Component Set：`946:9650`
- 职责：在地图上标记取件地或收件地。

#### 尺寸行为

- 宽度：Fixed
- 高度：Fixed
- referenceSize：`30 × 38.25px`

#### 变体与槽位

- 变体轴 `Property 1`：地标取/普通、地标收/普通、地标取/尊贵、地标收/尊贵。
- 可替换槽位：无自由文字槽位。
- 稳定差异：上方圆标、下方杆和圆点构成不可拆分的整体。

### 下单页地图标签

#### 组件身份

- 名称：下单页地图标签
- Figma Component Set：`946:9401`
- 职责：在地图气泡中组合接单或送达时间与地址信息。

#### 尺寸行为

- 宽度：Hug
- 高度：Hug
- referenceSize：随地址内容和变体变化

#### 变体与槽位

- 变体轴 `Property 1`：最快接单、预计送达/普通、预计送达/尊贵。
- 可替换槽位：时间、状态标题、地址、联系人、电话和右侧入口。
- 稳定差异：左侧表达时间语义，右侧承载地址信息；尊贵态改变承载面。

### 下单页物品信息确认

#### 组件身份

- 名称：下单页物品信息确认
- Figma Component Set：`1113:12122`
- 职责：在下单确认阶段汇总物品摘要、保价和备注信息。

#### 尺寸行为

- 宽度：Fill
- 高度：Hug
- referenceSize：当前示例宽 `359px`

#### 变体与槽位

- 变体轴 `Property 1`：下单页物品确认1、下单页物品确认2。
- 可替换槽位：物品摘要、保价组件、备注和右侧入口。
- 稳定差异：已保价态使用收敛摘要；未保价态展示完整建议区域。

### 保价

#### 组件身份

- 名称：保价
- Figma Component Set：`1496:29632`
- 职责：展示保价方案、建议、权益和已选摘要。

#### 尺寸行为

- 宽度：Fill
- 高度：Hug
- referenceSize：展开态 `327 × 139px`，收敛态约 `327 × 41px`

#### 变体与槽位

- 变体轴 `Property 1`：未保价-提示、未保价-建议、已保价-权益、已保价-收敛。
- 可替换槽位：品牌标题、权益提示、保价方案、自定义价值入口和收敛摘要。
- 稳定差异：未保价态展示选择方案；已保价态突出权益或收敛为摘要。

### 骑手卡片

#### 组件身份

- 名称：骑手卡片
- Figma Component Set：`1507:20966`
- 职责：展示骑手身份、评分和联系操作，并可附加打赏提示。

#### 尺寸行为

- 宽度：Fill
- 高度：Hug
- referenceSize：基础态 `359 × 74px`，打赏态 `359 × 105px`

#### 变体与槽位

- 变体轴 `Property 1`：骑手卡片、骑手卡片-打赏。
- 可替换槽位：头像、姓名、平台标识、满意度、评分、消息、电话和打赏文案。
- 稳定差异：打赏态在基础卡片下方增加独立提示条。

### 下单页服务选择

#### 组件身份

- 名称：下单页服务选择
- Figma Component Set：`864:6906`
- 库中实例：`1540:27741`
- 职责：比较普通帮送、1 对 1 急送和汽车配送的服务、金额与时间。

#### 尺寸行为

- 宽度：Fill
- 高度：Hug
- referenceSize：当前变体宽 `359px`，高度约 `126–240px`

#### 变体与槽位

- 变体轴 `决策因子`：服务+时间、金额+时间、服务+金额。
- 变体轴 `列表/tab`：列表、tab、2.5。
- 可替换槽位：服务名称、服务标识、权益标签、价格、送达时间、帮助入口和载具页签。
- 稳定差异：`列表` 为纵向服务列表；`tab` 增加载具页签；`2.5` 为横向卡片布局。

### 物品凭证

#### 组件身份

- 名称：物品凭证
- Figma Component Set：`1541:28177`
- 职责：展示取收物品照片、动态收货码和异常处理入口。

#### 尺寸行为

- 宽度：Fill
- 高度：Fixed
- referenceSize：`359 × 102px`

#### 变体与槽位

- 变体轴 `Property 1`：待取件、待收件、已送达。
- 可替换槽位：保障标题、收货码、说明、取件照片、收件照片和异常处理入口。
- 稳定差异：收货码由隐藏到显示；照片随配送阶段逐步补齐。

### 提交订单按钮

#### 组件身份

- 名称：提交订单按钮
- Figma Component：`1554:4789`
- 职责：承载优惠、支付方式、价格和最终提交操作。

#### 尺寸行为

- 宽度：Fill
- 高度：Fixed
- referenceSize：`375 × 120px`

#### 变体与槽位

- 变体：无。
- 可替换槽位：优惠状态、支付方式、现价、原价、找人付和提交订单。
- 稳定差异：底部操作区包含 Home Indicator 安全空间，不能只实现中间按钮。

### 骑手情感语录

#### 组件身份

- 名称：骑手情感语录
- Figma Component Set：`1577:20555`
- 职责：通过骑手形象与气泡文案反馈物品尺寸风险。

#### 尺寸行为

- 宽度：Fixed
- 高度：Fixed
- referenceSize：约 `120 × 225–226px`

#### 变体与槽位

- 变体轴 `Property 1`：这件交给我，您放心！、这件有点大，汽车送更稳、太大啦，可能送不了哦。
- 可替换槽位：无自由组合槽位；骑手形象与气泡文案按状态成对切换。
- 稳定差异：三种状态分别表达正常、建议汽车和超限风险。

### 骑手状态

#### 组件身份

- 名称：骑手状态
- Figma Component Set：`1812:15204`
- 职责：在配送追踪中展示当前阶段、时间或距离及状态说明。

#### 尺寸行为

- 宽度：Hug
- 高度：Hug
- referenceSize：随状态文案和数值变化

#### 变体与槽位

- 变体轴 `Property 1`：待接单、取件中、送件中、达到送件地。
- 可替换槽位：时间或分钟、阶段标题、状态说明、距离和右侧入口。
- 稳定差异：待接单显示倒计时；后续状态显示距取件地或送件地的时间与距离。

## 4. 该做 / 不该做 · Do's and Don'ts

### Do

- 项目启动时先读取 Figma Variables、Text Styles 和公共组件库，再建立工程 Token。
- 保留 Figma 源名称与代码映射名的对应关系，名称不一致时明确记录原因。
- 公共组件契约只记录身份、尺寸行为、变体和可替换槽位。
- 将当前实例尺寸标记为 `referenceSize`，用于视觉核对而不是固定实现。
- 默认使用 Fill 宽度和 Hug 高度，让内容、变体和文本换行驱动布局。
- 只为导航栏、按钮、输入框、页签、进度节点等稳定交互控件使用固定高度。
- 开发具体页面时重新读取目标页面节点，核对局部间距、文案、素材锚点和组件实例覆盖。
- 组件外边距由父级布局管理；组件内部 padding 和 gap 由组件自身管理。
- 复用已有公共组件及其变体，不在页面中重复绘制同一语义结构。
- SVG 和图片保持宽高比；固定品牌字优先使用轮廓 SVG。
- Token 或组件源发生变化时，同步更新 Figma、本文档和工程映射。

### Don't

- 不在项目初始文档中猜测 Token 会出现在哪些具体页面。
- 不把产品业务规则、页面交互条件或研发架构写入设计系统。
- 不把一个复杂组件概括成单一字号、字重或 Typography。
- 不把 Figma 中某个实例的像素宽高直接写成所有屏幕的固定尺寸。
- 不因为 Figma 旧组件设置为 Fixed，就机械复制其固定高度。
- 不把父级外边距、页面绝对坐标或局部覆盖值写成组件固有属性。
- 不从截图目测颜色、字号、行高、圆角和间距。
- 不用整张截图或占位图片替代应由真实元素和公共组件构成的界面。
- 不非等比拉伸 SVG、品牌字、图标、人物或地图素材。
- 不在组件库未定义的情况下自行增加变体、状态或语义颜色。
- 不让固定底栏遮挡内容，也不遗漏系统安全区。

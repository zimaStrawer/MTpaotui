# 跑腿改版原型：产品与业务需求（v1）

> 本文是项目的产品事实来源，回答“产品提供什么能力、业务状态如何变化、用户操作后发生什么”。
> 页面视觉、Token 和组件尺寸见 `design.md`；代码架构和实现约束见 `MTprototype-tech-spec.md`。

---

## 0. 文档边界

三个核心说明文档分别对应互联网产品协作中的三个角色：

| 角色 | 文档 | 负责内容 |
|---|---|---|
| 产品 | `product.md` | 业务目标、领域对象、业务规则、用户流程、页面交互、状态机、验收口径 |
| 设计 | `design.md` | 设计理念、Design Token、字体、颜色、间距、组件规格、响应式与视觉还原 |
| 研发 | `MTprototype-tech-spec.md` | 技术栈、架构、目录、状态与数据实现、接口边界、测试和部署 |

归类原则：

- “超过什么条件推荐汽车”属于产品业务规则。
- “用户切换服务时地址如何变化”属于产品交互规则。
- “按钮是 44px 高、使用哪个颜色 Token”属于设计规则。
- “用 Zustand 还是 Context、Repository 如何注入”属于研发规则。

---

## 1. 产品定位

### 1.1 目标

本项目改版美团跑腿“帮取送”核心链路，用于：

1. 完成 n=8 的可用性测试，采用拉丁方设计，以“七夕送花”为场景锚点。
2. 验证用户委托陌生人配送重要物品时，对“确定性”的需要是否高于单纯追求速度。
3. 通过完整原型呈现从下单到签收的信任建立过程。

### 1.2 产品命题

围绕“委托信任三角”组织体验：

- **物可见**：持续展示物品类型、物品凭证、取件照片、送达照片和收货码。
- **人可信**：展示骑手身份、评分、满意度和信用徽章。
- **事可感**：展示接单预期、配送阶段、预计时间、剩余距离和环境提示。

### 1.3 本期范围

包含：

- 帮送、帮取、1 对 1 急送。
- 取件与收件地址填写。
- 物品类型、重量、体积、备注和保价。
- 普通、急送、汽车三类配送服务。
- 下单确认、提交订单和自动推进的配送追踪。
- 订单完成与物品签收凭证。

不包含：

- 帮我买、帮个忙的完整流程。
- 真实登录、支付、地图、地址识别和后端服务。
- 订单取消、无人接单、地址识别失败、骑手异常等失败分支。
- 地址簿搜索、左滑置顶/删除等非核心操作的完整实现。

---

## 2. 领域模型

领域模型描述业务中的稳定对象和状态，不涉及代码框架或页面尺寸。

### 2.1 业务与服务

| 对象 | 可选值 | 说明 |
|---|---|---|
| 业务类型 | 帮取送、帮我买、帮个忙 | 本期仅帮取送可进入核心流程 |
| 服务模式 | 帮送、帮取、1 对 1 急送 | 决定地址语义、载具能力和服务权益 |
| 配送载具 | 二轮车、汽车 | 1 对 1 急送不允许用户切换 |
| 配送服务 | 普通帮送、1 对 1 急送、汽车配送 | 下单确认页的最终计价档位 |

### 2.2 地址

一个地址包含：

- 地址角色：取件或收件。
- 地图定位点（POI）。
- 门牌号。
- 联系人姓名。
- 11 位手机号。

一笔订单必须同时具有取件地址和收件地址。

### 2.3 物品

一个物品包含：

- 物品类型。
- 重量（kg）。
- 长、宽、高（cm）。
- 保价档位。
- 可选的物品描述或配送要求。

支持 11 类物品，顺序固定为：

餐饮、文件、生鲜、蛋糕、鲜花、数码、服饰、帮取快递、五金、汽配、其他。

鲜花、蛋糕、数码属于易损品类。

### 2.4 保价

| 档位 | 产品含义 |
|---|---|
| 未保价 | 最高赔付 5 倍配送费 |
| ¥1 | 物品价值 500 元及以下 |
| ¥2 | 物品价值 501～1000 元 |
| 自定义 | 用户声明自定义物品价值；当前原型只完成档位选择 |

### 2.5 订单

订单由以下信息组成：

- 业务类型与服务模式。
- 取件地址与收件地址。
- 物品信息。
- 配送载具。
- 实付金额。
- 提交后由服务端或 Mock 分配的订单号、骑手和 4 位收货码。

### 2.6 配送阶段

配送追踪包含六个技术阶段：

`等待接单 → 已接单 → 已取件 → 配送中 → 到达收件地 → 已完成`

Figma 使用五套主要页面表现；“已取件”和“配送中”复用同一页面骨架，通过文案、进度和地图位置区分。

---

## 3. 核心业务规则

### 3.1 服务模式与地址

1. 帮送与帮取直接互相切换时，取件地址和收件地址交换。
2. 地址交换后，地址本身的角色同步修正，不能只交换显示文字。
3. 从帮送或帮取切换到 1 对 1 急送时，两个地址保持不变。
4. 从 1 对 1 急送切回帮送或帮取时，也保持当前地址，不自动交换。
5. 只有帮送与帮取的直接互切触发地址交换动画。

### 3.2 载具与运力信息

- 帮送、帮取支持二轮车和汽车切换。
- 1 对 1 急送由平台锁定运力方案，不显示载具切换。
- 未填写取件地址时，无法计算运力，显示“填取件地址可查接单时间”。
- 已填写取件地址后：
  - 帮送/帮取 + 二轮车：显示预计接单运力信息。
  - 帮送/帮取 + 汽车：不显示右侧骑手运力信息。
  - 1 对 1 急送：显示急送运力信息，不受草稿中遗留载具影响。

### 3.3 汽车推荐机制

满足任意一项时，推荐汽车配送：

| 条件 | 当前原型阈值 |
|---|---:|
| 体积较大 | 长 × 宽 × 高 ≥ `50 × 40 × 40cm`（80,000cm³） |
| 物品过重 | 重量 ≥ `20kg` |
| 易损物品 | 鲜花、蛋糕、数码 |
| 距离较远 | 配送距离 ≥ `10km` |
| 天气恶劣 | 当前天气为雨天 |

规则性质：

- 这是推荐，不是强制改为汽车。
- 命中后应在下单确认页展示“推荐使用汽车配送”提示。
- 用户仍可在支持载具切换的服务模式中选择二轮车。
- 1 对 1 急送不参与载具推荐切换。

### 3.4 重量规则

- 默认重量为 `1kg`。
- 重量输入范围为 `0–20kg`。
- `5kg` 内不加价；超过后由骑手取件评估并补齐差价。
- 重量变化即时更新页面数值，但当前原型不计算真实超重费用。

### 3.5 体积与动态反馈

基础规则：

- 默认配送箱为 `41 × 30 × 31cm`。
- 长、宽、高各自输入范围为 `0–150cm`。
- 黄色三维框与三个输入值按 `0–150cm` 线性映射。
- 数值为 0 时对应边长为 0；数值为 150 时到达该轴终点。
- 点击“恢复默认”后回到 `41 × 30 × 31cm`。

状态与动态语录：

| 判定优先级 | 条件 | 骑手状态语录 |
|---:|---|---|
| 1 | 三边之和 > `150cm` | “太大啦，可能送不了哦” |
| 2 | 任意一边 > `100cm` | “这件有点大，汽车送更稳” |
| 3 | 其余情况 | “这件交给我，您放心！” |

当两个条件同时满足时，超限状态优先于汽车建议状态。

### 3.6 保价规则

- 易损物品未保价时，提示“物品易损 建议您保价”。
- 普通物品未保价时，提示“未保价最高赔付 5 倍配送费”。
- 选择任一保价档位后，展示高信用骑手与全额赔权益。
- 再次点击当前已选档位可取消保价，回到未保价。
- 在物品页选择的保价、备注必须同步到下单确认页。
- 下单确认页中，已保价默认显示缩小态，用户可展开修改。

### 3.7 物流物品凭证优先级

物品凭证顶部服务标识按以下优先级展示：

`1 对 1 急送 > 保价服务 > 普通帮送`

因此，同时选择急送和保价时显示急送标识；汽车配送暂沿用普通帮送标识。

---

## 4. 核心用户流程

1. 用户从首页选择帮送、帮取或 1 对 1 急送。
2. 点击“去下单”：
   - 缺取件地址时先进入取件地址页。
   - 已有取件地址但缺收件地址时进入收件地址页。
   - 两个地址齐全后进入物品信息页。
3. 用户选择物品类型，填写重量、体积、备注和保价。
4. 用户在下单确认页核对地址、物品、配送服务和费用。
5. 用户提交订单后进入配送追踪页。
6. 配送阶段自动推进，最终到达订单完成页。

流程守卫：

- 地址未齐或物品未填时，不能直接进入下单确认页。
- 没有订单回执时，不能直接进入配送追踪页。
- 前置数据缺失时返回首页重新开始核心流程。

---

## 5. 全局交互规则

### 5.1 非核心功能

- 非核心功能统一弹出：“该功能暂未开放，敬请期待”。
- Toast 使用同一组件、同一 Token、同一出现和消失方式。
- Toast 默认展示约 `3s`。
- 电话、消息、客服、收藏、分享、理赔、修改完成订单、查看详情等未进入本期核心链路的操作都遵循该规则。
- 打赏等已有明确结果反馈的动作可以使用场景文案，但必须复用同一 Toast 组件、Token 和动效，不得另做样式。

### 5.2 状态连续性

- 服务模式、地址、物品、保价和载具在页面跳转间保持。
- 从下单确认页返回地址页修改，保存后回到下单确认页。
- 从下单确认页返回物品页修改，确认后仍回到下单确认页。
- 刷新或重新打开原型允许回到干净起点，不要求持久化历史订单。

### 5.3 动效原则

- 动效用于解释状态关系，不改变业务结果。
- 页签和载具切换使用水平位移动效。
- 帮送/帮取互切时只有地址内容移动，取/收标签保持原位。
- 展开详细体积后自动滚动到体积模块，避免用户再次寻找。
- 用户开启“减少动态效果”时，自动滚动和位移动效可降级为立即切换。

---

## 6. 页面需求

### 6.1 首页

业务入口：

- “帮取送”为唯一可进入核心流程的业务。
- “帮我买”“帮个忙”和底部非核心页签点击后显示未开放 Toast。

服务页签：

- 包含帮送、帮取、1 对 1 急送。
- 页签选中背景平移至目标位置。
- 1 对 1 急送切换时同步切换顶部 Banner 变体。
- 帮取送与急送 Banner 文案切换时，左侧文案使用横向进入动效。

地址与运力：

- 地址未填写时显示角色对应占位文案。
- 帮送/帮取互切执行 §3.1 的地址规则。
- 载具和运力执行 §3.2 的显隐规则。

下单：

- 起步价 Mock 为 ¥14.5，展示“最高减 20 元”。
- 点击后按 §4 的缺失信息顺序继续流程。

### 6.2 地址填写页

- 取件和收件复用同一页面，根据地址角色切换标题、标签、地图标识和默认测试数据。
- 地址与电话必填；电话必须为 11 位数字。
- 门牌号和联系人当前不阻止保存。
- 点击“选择取件地址/选择收件地址”自动填充对应场景的完整默认信息，便于后续可用性测试。
- 点击地址簿条目后，将该条目的地址、门牌号、联系人和电话回填到表单。
- 地图气泡显示当前表单中的 POI，内容变化后保持同步。
- 顶部粘贴识别属于非核心功能，点击整行弹出未开放 Toast。
- 保存后：
  - 从下单确认页进入时，返回下单确认页。
  - 另一角色地址已存在时，进入物品信息页。
  - 另一角色地址不存在时，返回首页继续补齐。

### 6.3 物品信息页

物品类型：

- 初始只展示类型选择。
- 选择任一类型后切换到详情状态。
- 用户可点击已选类型重新打开类型宫格。
- “上传照片识别物品”和“禁送清单”属于非核心能力，应使用统一未开放提示。
- 七夕测试场景选择鲜花后展示花束 Mock 照片；其他类型不展示该照片。

详情：

- 备注可填写物品描述和配送要求；空白内容不写入订单。
- 重量遵循 §3.4。
- 体积遵循 §3.5。
- 保价遵循 §3.6。

确认：

- 未选类型时确认按钮不可用。
- 点击确认后一次性保存完整物品信息并进入下单确认页。

### 6.4 下单确认页

服务选择：

- 首页急送进入时，预选 1 对 1 急送。
- 首页选择汽车时，预选汽车配送。
- 其他情况预选普通帮送。
- 用户在本页重新选择服务后，需要回写同一订单草稿：
  - 选择急送：服务模式改为急送，载具归为二轮车占位值。
  - 选择汽车：若原来为急送则退回帮送，载具改为汽车。
  - 选择普通：若原来为急送则退回帮送，载具改为二轮车。

车型推荐：

- 根据 §3.3 计算推荐载具。
- 命中汽车推荐时展示提示，但不自动替用户改选。

信息编辑：

- 点击取件/收件地图信息可回对应地址页修改。
- 缩略地图支持交换取件与收件地址。
- 物品摘要、备注和保价与物品页使用同一份草稿。

页面滚动：

- 当主地图大部分离开视口后，显示吸顶的导航公告和缩略地图。
- 公告内容按 Mock 顺序轮换，限时优惠倒计时实时递减。

提交：

- 点击提交后进入“提交中”，防止重复提交。
- 提交成功后保存订单回执并进入配送追踪。
- “找人付”、红包、支付方式、取件时间、小费等当前为非核心占位。

### 6.5 配送追踪页

配送追踪是单一路由内的状态机，不为每个阶段建立新页面。

等待接单：

- 展示 `44s` 倒计时。
- `44s` 表示预计接单窗口；压缩测试中骑手可在倒计时结束前接单，因此等待阶段可在 `10s` 后提前结束。
- 中心角色周围依次向外扩散雷达波纹。
- 此阶段不展示完整骑手卡片和物品凭证。

接单后：

- 展示骑手身份、评分、满意度和徽章。
- 展示物品凭证与收货码状态。
- 地图上展示骑手、阶段目标点、预计分钟和剩余距离。
- 每 `2s` 使用同一份路线快照同步更新骑手位置、预计分钟和距离。

阶段变化：

- 进度、时间文案、地图目标、骑手状态、收货码和物品凭证必须由同一个配送阶段派生。
- 阶段切换后从新阶段的路线起点开始，不能发生地图倒退。
- 订单完成后停止阶段推进和位置刷新。

完成页：

- 展示订单已完成、服务距离、服务时长、签收方式、完整取收凭证、骑手卡片和订单信息。
- 完成态的打赏提示可以关闭。
- 所有未纳入核心流程的操作使用统一未开放 Toast；打赏结果反馈允许使用成功文案，但不得使用另一套 Toast 样式。

---

## 7. 配送追踪状态机

### 7.1 阶段与时间

| 阶段 | Mock 停留 | 状态文案 | 凭证/收货码 |
|---|---:|---|---|
| 等待接单 `accepting` | 10s | 正在为您召唤骑士 | 不展示物品凭证 |
| 已接单 `accepted` | 12s | 预计取件，请准备物品 | 取件未确认，收货码为掩码 |
| 已取件 `picked` | 12s | 骑手送件中，预计送达 | 取件凭证确认，揭示 4 位收货码 |
| 配送中 `delivering` | 12s | 骑手送件中，预计送达 | 保持取件确认，向收件地移动 |
| 到达 `arrived` | 10s | 已到达收件地 | 收件凭证确认，距离为 0 |
| 已完成 `completed` | 终态 | 订单已完成 | 展示完整取收凭证 |

### 7.2 进度条

| 阶段 | 进度 |
|---|---:|
| 等待接单 | 0% |
| 已接单 | 28% |
| 已取件 | 75% |
| 配送中 | 75% |
| 到达 | 100% |
| 已完成 | 100% |

节点激活规则：

- 费用节点始终激活。
- 取件节点从“已取件”开始激活。
- 收件节点从“到达收件地”开始激活。

### 7.3 收货码

- 收货码由订单提交成功后分配，当前 Mock 为 `8251`。
- 在“已取件”之前显示 `✱✱✱✱`。
- 从“已取件”开始显示 4 位数字。
- 揭示后提示用户确认物品完好，再告知骑手收货码。

### 7.4 物品凭证

- “已接单”时：取件与收件凭证均为待确认。
- “已取件”时：取件凭证替换为确认照片。
- “到达收件地”时：收件凭证替换为确认照片。
- 已确认凭证支持展示放大入口，但当前点击使用未开放 Toast。

---

## 8. 原型 Mock 口径

这些数值用于可用性测试，不代表正式商业计价：

| 项目 | 当前值 |
|---|---|
| 普通帮送 | ¥4.9，划线原价 ¥15，预计 16:58 送达 |
| 1 对 1 急送 | ¥9.3，预计 16:40 送达 |
| 汽车配送 | ¥29.5，预计 16:45 送达 |
| 首页起步价 | ¥14.5 起 |
| 最快接单 | 3 分钟 |
| 地图预计送达 | 12:03 |
| 场景天气 | 雨天/强风 |
| 场景距离 | 3.2km |
| 骑手 | 谢师傅，4.5 分，满意度 97.73% |
| 骑手徽章 | 信用骑手、鲜花使者 |
| 完成态摘要 | 343 米、28 分钟 |

---

## 9. 产品验收口径

### 9.1 核心流程

- 能从首页依次完成取件地址、收件地址、物品信息、下单确认和配送追踪。
- 服务模式、载具、地址、物品、保价和备注在页面间保持一致。
- 页面不能绕过必要前置数据直接进入下单或追踪。

### 9.2 规则

- 帮送/帮取互切正确交换地址，急送切换保持地址。
- 运力信息在所有模式和地址状态下符合 §3.2。
- 汽车推荐五类触发条件均可独立命中。
- 体积三个状态及动态语录符合 §3.5 的优先级。
- 保价状态和物流凭证服务标识符合优先级规则。

### 9.3 追踪

- 一分钟左右可以自动看完完整配送过程。
- 收货码只在已取件后揭示。
- 骑手位置、预计分钟和距离每 2 秒同步变化。
- 完成后不再产生阶段或位置更新。

---

## 10. 需求与当前实现差异

本轮检查发现以下产品需求已经明确，但工程实现仍不完整：

1. **汽车推荐尚未接入页面**：推荐函数和阈值已经存在，但下单确认页当前没有调用，也没有展示推荐提示。
2. **追踪页非核心操作尚未完全归一**：电话仍会发起 `tel:`，分享仍调用系统分享，收藏仍直接切换状态，消息/客服等使用各自文案；应按 §5.1 统一。
3. **物品照片识别与禁送清单缺少点击反馈**：当前主要是视觉展示，应补统一未开放 Toast。
4. **自定义物品价值没有数值输入**：当前只记录“custom”档位；若后续测试需要验证申报金额，需补输入与校验。
5. **地址簿搜索、左滑置顶/删除只是展示**：与本期范围一致，但需要避免让参与者误以为已经可用。
6. **完成态距离和用时为静态 Mock**：尚未由配送路线实际累计计算。

以上差异不改变本期核心流程，但后续开发应以本节作为补齐清单。

# 2026-07-24

## 分析 awesome-design-md 的章节结构（续昨日）

用户要求分析 DESIGN.md 文件内部结构与提取各章页面标题。

结论：仓库实际存在**两套主要章节模板**，且多数文件≠README 宣称的「9 段标准结构」。

**模板 A（主流，约占抽查的 9/10）**：描述式、未编号。章节顺序：
`# Language`(新文件才有) → `## Web Content to Analyze`(新文件才有) → `## Overview` → `## Colors`(Brand&Accent/Surface/Text/Semantic) → `## Typography`(Font Family/Hierarchy/Principles/Note on Font Substitutes) → `## Layout`(Spacing/Grid/Whitespace) → `## Elevation & Depth`(Decorative Depth) → `## Shapes`(Border Radius/Photography Geometry) → `## Components`(Buttons/Cards/Inputs/Navigation + 站点定制块) → `## Do's and Don'ts`(Do/Don't) → `## Responsive Behavior`(Breakpoints/Touch/Collapsing/Image) → `## Iteration Guide` → `## Known Gaps`(可选)。

**模板 B（仅 Tesla 明确使用，对应 README 宣称）**：编号 1-9：`Visual Theme & Atmosphere` / `Color Palette & Roles` / `Typography Rules` / `Component Stylings` / `Layout Principles` / `Depth & Elevation` / `Do's and Don'ts` / `Responsive Behavior` / `Agent Prompt Guide`。

关键发现：两套模板 8 项核心重合（命名略异），但模板 A 额外有 `Shapes`、`Language`、`Web Content`、`Known Gaps` 四块；README 的 9 段并未被多数文件采用 → 印证昨日「格式一致性 6/10」的判断。

## 分析本项目 design.md 结构（用户 @design.md 要求）

项目自身的 /Users/zimastraw/Documents/MTpaotui/design.md（name: Meituan-Errand-Redesign）整体沿用 awesome 模板 A 骨架，但做了增强：
- **双层结构**：① YAML frontmatter 存机器可读 token（colors/typography/rounded/spacing/components）；② Markdown 正文 11 章讲理念/规则/表格。比 awesome 库更利于 agent 解析。
- 相对模板 A 的本地化：Overview→frontmatter description + 独立`设计理念`章；新增`交付与集成 Handoff`章；组件章按「信任三角」重组；token 语义化（insurance/alert 标产品语义色）。
- 发现的不一致（待修）：① `{typography.display}` 正文引用但 frontmatter 未定义（Known Gaps 也承认）；② `DINPro` 在 Known Gaps 提及但全文无定义（孤儿引用）；③ 组件用中文作 YAML key 脆弱；④ 色值大小写不统一。
- 用户为「前端小白」，讲解偏类比、结合本项目实际代码。

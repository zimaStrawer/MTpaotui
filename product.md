# 美团跑腿体验优化：产品需求规范

> 本文是项目稳定的产品事实源，用于定义产品定位、核心流程、页面需求、领域规则、本期范围与里程碑。前半部分说明产品全貌，后半部分按开发进度维护页面需求；视觉与 Token 见 `design.md`，工程实现规则见 `tech.md`。

## 1. 核心产品定位

本项目是美团跑腿“帮取送”核心链路的体验改版原型，面向无法亲自完成物品交付的用户，通过清晰的服务选择、透明的配送过程和可验证的交付凭证，降低委托陌生骑手配送重要物品时的不确定感。

产品原则：物可见、人可信、事可感。

## 2. 目标用户与核心场景

### 目标用户

有即时取送需求，但因忙、急、宅、懒或忘带等原因，无法亲自完成交付的城市用户。

### 核心场景

为用户提供日常代取送、临时急送、忘带补送、礼赠配送及重要物品交付等同城即时配送服务。

- 临时急送：文件、证件、药品等需要尽快送达
- 代取代送：餐饮、母婴用品、图书文具等日常物品取送
- 忘带补送：钥匙、电脑、证件等遗漏物品临时补送
- 礼赠配送：鲜花、蛋糕、礼物等需要准时送达
- 重要物品配送：数码、珠宝、美妆等关注安全与交付凭证的物品

## 3. 核心使用流程

```text
首页选择服务
→ 填写取件与收件地址
→ 填写物品信息
→ 选择配送服务并确认费用
→ 提交订单
→ 查看配送进度
→ 确认交付结果
```

流程约束：

- 地址和物品信息未完成时，不能提交订单；
- 已填写的信息在页面跳转后保持一致；
- 订单提交后按照配送状态进入追踪和完成阶段。

## 4. 页面需求详述

页面需求与核心产品事实共存于本文。开发某一页面时，优先阅读该页面对应小节，并同时遵守第 10 节的跨页面规则。

| 页面 | 对应章节 |
|---|---|
| 首页 | §5 |
| 地址填写 | §6 |
| 物品信息 | §7 |
| 下单确认 | §8 |
| 配送追踪 | §9 |
| 跨页面领域与交互 | §10 |
| 订单完成 | §11 |

## 5. 首页需求

### 页面目标

让用户理解并选择帮送、帮取或 1 对 1 急送，查看地址和运力摘要，并进入核心下单流程。

### 进入条件

原型干净起点或用户从其他页面返回首页。已有订单时不要求持久化恢复。

### 页面信息

- 业务入口：帮取送可进入核心流程；帮我买、帮个忙和底部非核心页签保留视觉表现；
- 服务页签：帮送、帮取、1 对 1 急送；
- 取件/收件地址摘要、载具和运力信息；
- 起步价 Mock：¥14.5；优惠文案：最高减 20 元。

### 核心操作

- 切换服务页签；
- 选择支持的载具；
- 点击“去下单”进入地址、物品或确认页；
- 点击非核心入口获得统一“暂未开放”反馈。

### 业务规则

- 帮送与帮取直接互切时交换取件和收件地址，并同步交换地址角色；只有该操作使用地址交换动效；
- 从帮送/帮取切换到急送，或从急送切回帮送/帮取，地址保持不变；
- 帮送、帮取支持二轮车和汽车；急送由平台锁定运力，不显示载具切换；
- 无取件地址时显示“填取件地址可查接单时间”；有地址后按服务模式展示运力信息；
- 急送切换时同步切换顶部 Banner 变体。

### 状态与数据同步

缺少地址时不阻断进入流程，但“去下单”按顺序补齐取件地址、收件地址，再进入物品页。服务模式、载具、地址和订单草稿在首页与后续页面共用。

### 页面验收标准

- 用户能区分三种配送服务的基本差异并进入核心流程；
- 帮送/帮取互切正确交换地址，急送切换不交换地址；
- 地址缺失时能够按正确顺序继续，不出现死路；
- 非核心入口使用统一反馈且不阻断主流程。

## 6. 地址填写页需求

### 页面目标

让用户完成取件或收件地址，并清晰识别当前地址角色。

### 进入条件

由首页或下单确认页进入，页面通过地址角色区分取件和收件状态。

### 页面信息与操作

- 页面标题、标签、地图标识和占位文案；
- POI、门牌号、联系人和手机号；
- 地址簿条目和地图预览；
- 粘贴识别入口；
- 填写地址和电话，点击“选择取件地址/选择收件地址”使用对应测试场景默认信息；
- 点击地址簿条目回填完整地址，保存后返回来源页面或进入下一步。

### 业务规则与状态

- 取件地址和收件地址必须同时存在，地址与电话必填；电话必须为 11 位数字；
- 门牌号和联系人当前不阻止保存；
- 地图气泡始终展示当前表单中的 POI，并随内容同步；
- 顶部粘贴识别属于非核心能力，点击整行显示统一未开放反馈；
- 保存前校验不通过时保留当前输入并提示修正，不清空表单；
- 取件和收件复用同一表单结构，但地址角色不能混淆；
- 从下单确认页进入并保存后返回下单确认页；另一角色地址已存在时保存后进入物品页，否则返回首页继续补齐；
- 地址交换必须交换地址对象及其角色，而不只是交换显示文字。

### 页面验收标准

- 用户能明确识别正在填写的是取件还是收件地址；
- 必填校验有效，合法地址能保存；
- 默认场景数据、地址簿回填和地图气泡保持一致；
- 从确认页返回修改后，确认页展示最新地址。

## 7. 物品信息页需求

### 页面目标

让用户完成物品识别、尺寸重量、配送要求和保价信息，形成可供下单确认使用的完整物品草稿。

### 进入条件

取件和收件地址均已完成。缺少任一地址时回到地址流程，不允许绕过前置条件。

### 页面信息与操作

- 物品类型宫格：餐饮、文件、生鲜、蛋糕、鲜花、数码、服饰、帮取快递、五金、汽配、其他；
- 重量、长宽高、备注、保价、骑手状态语录、体积示意和保价权益；
- 七夕场景选择鲜花后展示花束 Mock 照片；
- 选择或重新选择物品类型，填写重量、长宽高和备注，恢复默认体积，选择或取消保价，接受或取消汽车配送推荐，确认完整物品信息。

### 业务规则与状态

- 未选物品类型时确认按钮不可用；选择类型后进入详情状态；
- 重量默认 小于4kg，输入范围 0–20kg；5kg 内不加价，超过后提示由骑手取件评估，原型不计算真实超重费用；
- 体积默认 41 × 30 × 31cm，长宽高各为 0–150cm；三维框按输入值线性映射；
- 体积语录优先级：三边之和 >150cm 显示“太大啦，可能送不了哦”；任一边 >100cm 显示“这件有点大，汽车送更稳”；其余显示“这件交给我，您放心！”；
- “太大啦，可能送不了哦”仅作为原型风险提示，不阻断物品保存和下单流程；取件地址、收件地址和物品信息完整时，点击确认仍进入下单确认页；
- 尺寸进入“汽车送更稳”状态时，在固定物品确认栏上方展示汽车配送推荐，推荐条吸附在“确定”按钮上方且不随页面滚动，右侧圆点默认选中；用户未取消并确认物品信息后，下单确认页自动选中汽车配送。用户主动取消后使用二轮车，继续调整尺寸时不得反复自动恢复选中；若尺寸恢复正常或进入超限状态，推荐组件隐藏且不改写原有配送选择；
- 鲜花、蛋糕、数码属于易损品；易损品未保价提示建议保价，普通物品未保价提示最高赔付 5 倍配送费；
- 选择已选保价档位可取消保价；选择保价后展示高信用骑手与全额赔权益；
- 照片识别和禁送清单为非核心能力，点击后显示统一未开放反馈；
- 备注为空时不写入订单；其他输入即时反映在页面，点击确认时一次性保存完整物品信息；自定义保价当前只记录档位，不录入金额；
- 物品类型、重量、体积、备注和保价由同一份订单草稿驱动；下单确认页可返回本页修改，保存后回到确认页。

### 页面验收标准

- 用户能够完成鲜花场景的物品填写并继续下单；
- 重量、体积和保价规则在边界值下表现正确；
- 保价和备注在确认页保持一致；
- 接受尺寸推荐后下单确认页自动选择汽车配送，主动取消后保持二轮车；
- 物品尺寸进入超限提示状态后仍能正常进入下单确认页；
- 未完成物品信息时不能提交订单。

## 8. 下单确认页需求

### 页面目标

集中确认地址、物品、服务和费用，允许用户回到前置页面修改后提交订单。

### 进入条件与页面信息

取件地址、收件地址和物品类型均已完成。页面展示取件/收件地图与地址、物品摘要、备注、保价、凭证标识、三类配送服务的价格/时效/权益、推荐载具提示、公告、倒计时、费用明细和提交按钮。

### 核心操作

- 切换配送服务；
- 点击地址、物品摘要或地图返回修改；
- 交换取件与收件地址；
- 展开/修改保价；
- 提交订单。

### 业务规则与状态

- 首页急送进入时预选 1 对 1 急送；首页选择汽车时预选汽车；其他情况预选普通帮送；
- 重新选择服务时回写同一订单草稿：急送使用二轮车占位值；汽车将急送退回帮送并选择汽车；普通将急送退回帮送并选择二轮车；
- 汽车推荐由物品体积、重量、易损品类、配送距离或雨天任一条件命中；推荐不自动改选，提示必须可见；
- 点击取件/收件信息回对应地址页；地图与缩略地图均支持交换地址，交换后取收标签、地址内容和地图标记同步更新；
- 已保价默认显示缩小态，可展开修改；物品摘要、备注和保价与物品页共用同一草稿；
- 用户已选择任一保价档位时，底部提交栏上方吸附展示“省心送”保障标签；取消保价后标签立即隐藏，提交栏自身布局保持不变；
- 地图大部分离开视口后显示吸顶公告和缩略地图；公告按 Mock 顺序轮换，优惠倒计时递减；
- 提交按钮进入“提交中”状态，防止重复提交；提交成功后保存订单回执并进入追踪；找人付、红包、支付方式、取件时间和小费使用统一未开放反馈。

### 页面验收标准

- 用户能比较三类配送服务并完成选择；
- 从确认页返回修改后能够回到确认页且数据更新；
- 推荐提示命中时展示但不替用户切换载具；
- 已保价时提交栏显示保障标签，取消保价后标签消失；
- 重复点击提交不会创建多个订单。

## 9. 配送追踪页需求

### 页面目标

通过骑手、地图、阶段进度、物品凭证和收货码，让用户持续确认订单正在由谁、以什么状态配送。

### 进入条件与页面信息

订单提交成功并已有订单回执。缺少订单回执时回到首页，不允许直接打开追踪页。页面展示单一路由内的配送状态机、骑手身份/评分/满意度/专业标签、地图与阶段目标点、预计时间、剩余距离、取收凭证、收货码和非核心操作入口。

### 核心操作

- 查看配送阶段、地图和凭证；
- 在收货码揭示后确认物品完好并告知骑手；
- 查看完成页摘要；
- 点击非核心操作获得统一未开放反馈。

### 业务规则与状态机

状态顺序为：`等待接单 → 骑手取件中 → 骑手送件中 → 到达收件地 → 订单完成`。技术上使用 `accepting → accepted → delivering → arrived → completed`。“骑手取到物品”是 `accepted → delivering` 的阶段切换事件，不单独停留为一个状态。

| 阶段 | Mock 停留 | 进度 | 凭证与收货码 |
|---|---:|---:|---|
| 等待接单 | 10s | 0% | 不展示物品凭证，收货码不展示 |
| 骑手取件中 | 12s | 28% | 取/收件待确认，收货码掩码 |
| 骑手送件中 | 12s | 75% | 取件凭证确认，揭示 4 位收货码 |
| 到达收件地 | 10s | 100% | 收件凭证确认，距离为 0 |
| 已完成 | 终态 | 100% | 展示完整取收凭证 |

- 等待接单展示 44s 预计接单窗口，但压缩测试中可在 10s 后提前结束；
- 取件和送件各使用 6 个路线快照，每 2s 同步更新骑手位置、预计分钟和距离，各持续 12s；
- 取件路线从 650m/5 分钟更新至 0m/0 分钟，送件路线从 1.8km/12 分钟更新至 0m/0 分钟；
- 取件阶段右下角只显示“取”，送件及到达阶段右下角只显示“收”；
- 进入送件阶段时，取件凭证确认并揭示收货码；地图切换到送件视角，骑手回到画面起点后重新向右下角收件地移动；
- 距离小于 1km 时使用“米”，大于或等于 1km 时使用一位小数的“公里”；
- 进度、时间文案、地图目标、骑手状态、收货码和凭证必须由同一阶段派生；
- 取件完成后骑手回到画面起点是地图重定位的预期表现，不代表骑手真实倒退；完成后停止推进和位置刷新；
- 收货码 Mock 为 `8251`，送件阶段前显示 `✱✱✱✱`，进入送件阶段后显示 4 位数字；
- 急送优先于保价服务成为物品凭证顶部服务标识，汽车沿用普通帮送标识。
- 骑手卡片的专业标签由当前订单物品类型派生：鲜花显示“鲜花使者”，蛋糕显示“蛋糕天使”，文件显示“文件保镖”，其他品类统一显示“美团跑腿”。

### 页面验收标准

- 约 44s 可以自动看完完整配送过程；
- 收货码只在进入送件阶段后揭示；
- 骑手位置、预计分钟和距离每 2 秒同步变化；
- 取件和送件均从画面左上侧起点向右下角单一目标点移动，阶段切换时地图视角明显变化；
- 完成后不再产生阶段或位置更新；
- 非核心操作不会离开核心追踪流程。

## 10. 跨页面领域与交互规则

### 领域对象

- 业务类型：帮取送、帮我买、帮个忙；本期只有帮取送进入核心流程；
- 服务模式：帮送、帮取、1 对 1 急送；服务模式决定地址语义、载具能力和服务权益；
- 配送载具：二轮车、汽车；1 对 1 急送由平台锁定运力；
- 配送服务：普通帮送、1 对 1 急送、汽车配送；这是下单确认页的最终计价档位；
- 地址包含取件/收件角色、地图定位点、门牌号、联系人和 11 位手机号；
- 订单包含业务类型、服务模式、取收地址、物品、载具、实付金额，以及提交后分配的订单号、骑手和 4 位收货码；
- 物品包含类型、重量、长宽高、保价档位和可选备注；鲜花、蛋糕、数码属于易损品；
- 保价档位：未保价、¥1、¥2、自定义；自定义当前只完成档位选择。

### 跨页面业务不变量

- 地址、物品、服务、保价、载具和备注在页面跳转间保持一致；
- 帮送与帮取直接互切时交换取件和收件地址，并同步修正地址角色；
- 从帮送/帮取切换到急送，或从急送切回帮送/帮取，地址保持当前顺序；
- 服务模式和载具共同决定最终配送服务；急送不受草稿中遗留载具影响；
- 服务模式同时决定全链路视觉主题：帮送/帮取使用普通黄色主题，1 对 1 急送使用尊贵主题；首页或下单确认页切换服务后，地址、物品、确认、追踪和完成页立即保持同一主题；
- 尊贵主题用于急送身份和订单级服务表达，包括急送入口、取收标识、地图标、下单服务选中态、提交区、凭证标题、搜索氛围，以及“骑手送件中”和“已到达送件地”的状态承载面；地址保存按钮、重量与体积控件、保价选中态、物品确认按钮、配送进度条和完成态打赏提示继续使用主题黄。美团品牌标识、价格橙、警示红、保价绿、中性色和禁用态同样保持原有语义；
- 没有完整地址和物品信息时，不能进入下单确认或提交订单；没有订单回执时，不能进入追踪；
- 物流页的地图、阶段文案、进度、骑手状态、物品凭证和收货码必须由同一订单阶段派生。
- 骑手专业标签必须由订单物品类型派生并随物品修改同步更新，不写入独立状态或固定在骑手 Mock 数据中。

### 运力与汽车推荐

- 未填写取件地址时显示“填取件地址可查接单时间”；
- 帮送/帮取 + 二轮车显示预计接单运力信息；帮送/帮取 + 汽车不显示右侧骑手运力；急送显示急送运力信息；
- 汽车推荐命中任一条件即可触发：长 × 宽 × 高 ≥ 50 × 40 × 40cm（80,000cm³）、重量 ≥ 20kg、物品为鲜花/蛋糕/数码、距离 ≥ 10km 或天气为雨天；推荐不强制切换载具。

### 全局交互规则

非核心功能统一使用“该功能暂未开放，敬请期待”反馈，并复用同一 Toast 组件、Token、出现和消失方式，默认展示约 3s。电话、消息、客服、收藏、分享、理赔、修改完成订单、查看详情、照片识别、禁送清单等均遵循该规则。打赏可以使用结果文案，但必须复用同一 Toast 组件。

动效用于解释状态关系，不改变业务结果。页签和载具切换使用水平位移动效；帮送/帮取互切时只有地址内容移动，取/收标签保持原位；打开详细尺寸时先完成开关滑动，再展开内容并滚动到体积模块，关闭时先让开关滑回关闭位置，再收起详细内容；减少动态效果时降级为立即切换。动效不得成为继续流程的必要条件。

全链路视觉验收时，应分别走通普通与急送流程，并在下单确认页执行普通、急送和汽车互切；选中服务、页面主题和订单草稿必须同步变化，不得出现跨页面残留主题。

## 11. 订单完成页需求

### 页面目标

让用户确认签收结果、查看完整交付凭证和本次服务摘要，形成可信的订单闭环。

### 进入条件与页面信息

配送阶段进入 `completed` 终态。页面展示订单已完成状态、服务距离、服务时长、签收方式、取件照片、送达照片、收货码、骑手信息、订单摘要和完成态打赏提示。

### 核心操作与业务规则

- 查看签收状态和交付凭证；
- 关闭打赏提示；
- 点击其他订单操作时获得统一未开放反馈；
- 完成页只展示已完成订单的最终状态，不再推进配送阶段或刷新位置；
- 取件和收件凭证均应可见，收货码展示完整值；
- 打赏成功反馈可以使用场景文案，但必须复用统一 Toast 组件和视觉规范；
- 没有完成态订单时不能直接进入本页，应回到首页或追踪流程；
- 服务距离、时长、骑手、地址、物品和凭证均来自同一订单回执及配送阶段；当前 Mock 摘要为“约2.5公里、17分钟”。

### 页面验收标准

- 用户能直接确认订单已完成及签收方式；
- 取送凭证、收货码和骑手信息完整展示；
- 完成态不再发生配送状态变化；
- 非核心操作不会阻断查看交付结果。

## 12. 本期范围

本期范围用于约束产品里程碑：所有里程碑都围绕“帮取送”核心链路的可用性测试原型展开，不向真实交易产品扩展。

### 本期纳入

- 首页服务选择：帮送、帮取、1 对 1 急送，以及二轮车/汽车的服务匹配信息；
- 取件地址和收件地址填写、角色切换与地址同步；
- 物品类型、重量、体积、备注和保价信息；
- 普通配送、1 对 1 急送和汽车配送的服务比较、费用确认与下单提交；
- 基于 Mock 数据的配送追踪、骑手信息、地图位置、配送阶段、收货码和取送凭证；
- 订单完成页的签收结果、交付凭证和服务摘要；
- 七夕送花测试场景，以及不同屏幕尺寸下的核心流程验证。

### 本期不纳入

- 真实账号、支付、计价、地图、定位、路线计算和后端订单服务；
- 帮我买、帮个忙等非核心业务的完整流程；
- 订单取消、无人接单、地址识别失败、骑手异常等失败流程；
- 地址簿搜索、分享、收藏、理赔、历史订单等辅助能力；
- 真实赔付计算、真实履约数据和生产环境数据持久化。

### 范围判断原则

- 能直接支撑首页至订单完成的主流程，或支撑七夕送花可用性测试，才属于本期范围；
- 非核心入口可以保留必要视觉表现，但不扩展成完整业务流程；
- Mock 数据用于验证流程和体验，不代表正式商业规则；
- 新增能力如果会引入真实服务、失败分支或历史数据管理，应作为后续范围重新评估。

## 13. 项目里程碑

本节定义产品层面的阶段目标和完成标准，不记录具体代码任务或实时研发状态。

| 里程碑 | 产品目标 | 完成定义 |
|---|---|---|
| P0 产品基线 | 明确帮取送核心链路、目标用户、七夕送花测试场景和本期范围 | 产品定位、核心流程、业务边界和统一验收口径形成文档事实源 |
| P1 服务与地址 | 让用户完成服务选择、载具选择、取件地址和收件地址填写 | 用户能够区分帮送、帮取和 1 对 1 急送，完成两端地址填写并进入物品信息 |
| P2 物品与配送要求 | 让用户描述物品并理解重量、体积、保价和骑手配送建议 | 用户选择物品类型、填写重量与体积、选择保价，并获得对应的配送状态提示 |
| P3 下单确认 | 汇总订单信息，帮助用户确认服务、费用和配送方式 | 地址、物品、服务、载具、保价和费用保持一致，用户可以提交订单 |
| P4 配送追踪 | 让用户持续了解骑手、位置、阶段、时间、距离和物品状态 | 订单从等待接单推进到完成，骑手位置与时间距离同步变化，收货码和凭证按阶段展示 |
| P5 完成交付 | 通过签收结果和取送凭证形成可信的订单闭环 | 用户能够确认订单完成，查看完整凭证、收货码、骑手和订单摘要 |
| P6 全链路验证 | 验证核心流程在不同屏幕尺寸和连续操作下保持稳定 | 320px–448px 页面可用，首页至完成页无死路、数据不丢失，非核心功能不阻断主流程 |
| P7 PWA 交付 | 让核心流程可以作为移动端应用形态独立使用 | 用户可以将原型添加到手机主屏，以独立窗口打开，并在 PWA 环境中走通核心流程 |

里程碑完成以“产品结果可被用户走通并符合验收标准”为准，不以单个页面或单个组件完成作为阶段完成依据。
